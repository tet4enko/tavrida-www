import React, { useState } from 'react';
import cn from 'classnames';
import styles from './index.module.scss';

const component = () => {
    const a = 1;
    return (
        <div className={cn({ [styles.Table]: true })}>
            ggwp
        </div>
    );
};

export default component;
